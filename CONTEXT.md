# Meow Memorizing

浏览器扩展：在网页上高亮用户维护的单词表，并在选中/悬停单词时展示翻译面板。

## Language

**已记住**:
用户认为自己已经掌握、主动从词汇书移除的单词。移除后该词在页面上不再高亮；但再次选中这个词时，行为保持现状——仍会像未处理过的词一样立即弹出翻译面板，不受"停用词"的圆点行为影响。存储字段沿用历史命名 `isDeleted`。
_Avoid_: 删除、移除（单独使用容易和"停用词"混淆）

**停用词**:
用户主动标记为"这个词根本不需要翻译"的单词（例如 is、the 这类过于简单的词）。标记后页面上不再高亮该词；再次选中停用词时不会立即弹出翻译面板，而是显示"重译提示点"。与"已记住"是完全独立的状态，两者互不影响、互不派生。
_Avoid_: 永久删除、黑名单词

**查无翻译**:
系统尝试翻译后发现词典查不到结果，自动打上的临时状态，非用户主动行为。处于该状态的词选中/悬停时完全没有任何弹窗（加载动画正常出现，但请求确认查无结果后面板静默关闭，不显示"未找到翻译"之类的文案），页面上也不高亮。冷却 3 天后，下次被选中/悬停会自动重新尝试翻译一次。没有面向用户的查看或手动设置入口。
_Avoid_: 自动删除（与"停用词""已记住"的用户主动语义不同）

**重译提示点**:
选中一个停用词时短暂出现的小圆点，出现/消失规则与普通翻译面板一致（不额外定时）。点击它会：取消该词的停用状态，并像正常单词一样弹出翻译面板。
_Avoid_: 小圆点、圆点提示（统一用这个术语）
